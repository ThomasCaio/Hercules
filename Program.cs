public static class Program {
    public static void Main(string[] args)
    {
        // Oraculo.Run.Default();
        // Constelacoes.Run.Default();
        // Soldados.Run.Default();
        // Formacao.Run.Default(10); // Parâmetro para o tamanho da matriz.
        // Corrida.Run.Default(2); // Parâmetro de iteração do somatório.
        // Conversoes.Run.Default(5); // Parâmetro de valor para a conversão.
        // Busca.Run.Default();
        // Acusacoes.Run.Default();
        // ValorOuRef.Run.Default();
        // Soma.Run.Default();
        // Heron.Run.Default();
        Services.Run.Default();
    }
}